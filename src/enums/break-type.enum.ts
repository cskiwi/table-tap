export enum BreakType {
  MEAL = 'meal',
  REST = 'rest',
  SMOKE = 'smoke',
  PERSONAL = 'personal',
  EMERGENCY = 'emergency',
  TRAINING = 'training',
  MEETING = 'meeting'
}

export enum BreakStatus {
  SCHEDULED = 'scheduled',
  STARTED = 'started',
  COMPLETED = 'completed',
  MISSED = 'missed',
  INTERRUPTED = 'interrupted'
}